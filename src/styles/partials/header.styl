.page-header
  box-shadow(0 10px 10px rgba(0, 0, 0, .25))

  height 100px
  margin-bottom 50px

  .top-nav
    clear()

    a
      display block
      float left
      padding 20px 40px
      text-align center

      &:hover
        background #eee
      
    
  
